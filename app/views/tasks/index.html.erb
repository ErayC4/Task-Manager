<p style="color: green"><%= notice %></p>

<h1>Tasks</h1>

<div id="tasks">
  <% @tasks.each do |task| %>
  <% if task.user == current_user %>    
    <%= render task %>
    
    <% end %>
  <% end %>
</div>

<%= link_to "New task", new_task_path %>
