<!DOCTYPE html>
<html>
  <head>
    <title>Timemanagement</title>
    <!-- meta for autosave -->
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <style>
      body {
        margin: 0;
      }
    </style>
  </head>

  <body>
    <%= render "home/navbar" %>
    <%= yield %>
  </body>
</html>
